2.27

(a)不合法：引用r必须绑定在对象上，不能是字面值或者表达式
(b)合法：常量指针可以指向常量对象，也尅指向非常量对象
(c)合法：常量引用初始化的对象可以是字面值或者任意表达式
(d)合法：p3是指向常量的常量指针，可以指向常量，也可指向非常量
(e)合法：p1是指向常量的指针，不限定其所指对象一定要是常量
(f)不合法：引用必须初始化

2.28
(a)i不合法：是int类型对象，r是int型对象的引用，但r的引用必须是对象，不能是字面值
(b)不合法：p1是指向int型指针，p2是指向int型对象的常量指针，所以p2必须初始化
(c)不合法：ic是int型常量，ic必须初始化，r是常量引用
(d)不合法：p3是指向const int对象的常量指针，常量指针必须被初始化
(e)合法：p是指向const int对象的指针

2.29
(a)合法：常量仅仅在执行改变其本身的操作上起作用，当作为初始值时，无需关心其所初始化对象的类型
(b)不合法在哪？？？
(c)合法
(d)不合法：p3是常量指针，一但初始化就不能被改变
(e)不合法：p2是常量指针，一但初始化就不能被改变
(f)不合法：ic是常量，一但初始化就不能被改变