2.30

v2是顶层const,v2的值不能被改变
p1是指向int型的指针
r1是绑定int型的引用
p2是底层const,p2的值可以改变
p3是顶层const,是指向const int的常量指针
r2是底层const

2.31
r1=v2;不合法->不能将一个int&绑定到int常量上

p1=p2;不合法->p1/p2没有相同的底层const，且常量转换成非常量是不可以的
p2=p1;合法->非常量可以转换成常量
p1=p3;不合法，同p1=p2
p2=p3;合法，两者具有相同的底层const
